<div id="companion">
  <div class="home-button-companion">
      <%= link_to "< Home", root_path %>
    </div>
  <div class="createtrip-companion-container" data-controller="create-companion"  data-create-companion-trip-value="<%= @trip.id %>">
    <h3 class="text-center">Traveller Details</h3>

    <div class="wrapper-companion-field" data-create-companion-target="wrapper">
      <%= render 'add_field', trip: @trip, companion: @companion %>
    </div>

    <div class="add-field">
      <%# <button class="add-field-text" data-action="click->create-companion#addTravellerFields">
        + Add more travellers
      </button> %>
    </div>
  </div>
</div>
